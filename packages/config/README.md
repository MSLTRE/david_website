# @luibrand-tile/config

Placeholder for shared TypeScript, ESLint, and Prettier configuration that will
be extracted in a later stage. Stage 1 keeps configuration colocated with each
workspace package.
