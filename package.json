{
  "name": "luibrand-tile",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy:cloudflare": "pnpm --filter web build && pnpm dlx wrangler pages deploy apps/web/out --project-name luibrandtile",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "check": "pnpm typecheck && pnpm lint && pnpm build"
  },
  "devDependencies": {
    "turbo": "^2.5.8",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
