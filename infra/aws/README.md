# infra/aws

Placeholder for future AWS infrastructure (SST or CDK).

**AWS infrastructure is intentionally not implemented in Stage 1.** This
directory exists to reserve the location so later stages — customer portal,
admin dashboard, self-storage backend, accounting backend — can drop a real
SST or CDK project in here without disturbing the website's repository shape.
